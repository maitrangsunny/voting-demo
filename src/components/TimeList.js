import React, { Component } from 'react';
import { Timer } from './Timer';

export class TimeList extends Component {
    render(){
        return(
            <div>
                <Timer/>
            </div>
        )
    }
}